{
  "name": "mission-exploit-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for Mission Exploit 2.0",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.3",
    "graphql": "^16.12.0",
    "types": "^0.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.2.2"
  }
}
